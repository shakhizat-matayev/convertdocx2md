### This config file contains required core defaults that must be set, along with a handful of common optional settings.
### For a full list of available settings, see https://microsoft.github.io/graphrag/config/yaml/

### LLM settings ###
## There are a number of settings to tune the threading and token limits for LLM calls - check the docs.

completion_models:
  default_completion_model:
    model_provider: azure
    model: ${AZURE_OPENAI_CHAT_DEPLOYMENT} 
    auth_method: api_key # or azure_managed_identity
    api_key: ${GRAPHRAG_API_KEY} 

    api_base: ${AZURE_OPENAI_ENDPOINT}     # e.g. https://<resource>.openai.azure.com
    api_version: "${AZURE_OPENAI_API_VERSION}"  
    azure_deployment_name: ${AZURE_OPENAI_CHAT_DEPLOYMENT}  # your Foundry deployment name

    call_args:
      temperature: 0

    retry:
      type: exponential_backoff

embedding_models:
  default_embedding_model:
    model_provider: azure
    model: ${AZURE_OPENAI_EMBED_DEPLOYMENT}
    auth_method: api_key
    api_key: ${GRAPHRAG_API_KEY}
 
    api_base: ${AZURE_OPENAI_ENDPOINT}
    api_version: "${AZURE_OPENAI_API_VERSION}"
    azure_deployment_name: ${AZURE_OPENAI_EMBED_DEPLOYMENT} # your embedding deployment name

    retry:
      type: exponential_backoff

### Document processing settings ###

input:
  type: text # [csv, text, json, jsonl]
  #file_type: text
  base_dir: "input"
  file_pattern: ".*\\.md$$"
  #file_encoding: utf-8

chunking:
  type: tokens
  size: 1200
  overlap: 100
  encoding_model: o200k_base

### Storage settings ###
## If blob storage is specified in the following four sections,
## connection_string and container_name must be provided

input_storage:
  type: file # [file, blob, cosmosdb]
  base_dir: "input"

output_storage:
  type: blob
  connection_string: ${AZURE_STORAGE_CONNECTION_STRING}
  container_name: graphrag-artifacts
  base_dir: "runs/poc"

reporting:
  type: file # [file, blob]
  base_dir: "logs"

cache:
  type: json # [json, memory, none]
  storage:
    type: blob
    connection_string: ${AZURE_STORAGE_CONNECTION_STRING}
    container_name: graphrag-cache
    base_dir: "cache/poc"

    
vector_store:
  type: azure_ai_search
  url: ${AZURE_SEARCH_ENDPOINT}
  api_key: ${AZURE_SEARCH_ADMIN_KEY}
  index_schema:
    entity_description:
      index_name: entity_description
      id_field: id
      vector_field: vector
      vector_size: 1536
    community_full_content:
      index_name: community_full_content
      id_field: id
      vector_field: vector
      vector_size: 1536
    text_unit_text:
      index_name: text_unit_text
      id_field: id
      vector_field: vector
      vector_size: 1536

### Workflow settings ###

embed_text:
  embedding_model_id: default_embedding_model

extract_graph:
  completion_model_id: default_completion_model
  prompt: "prompts/extract_graph.txt"
  entity_types: [well, formation, member, lithology, production_metric, operational_anomaly, performance_status]
  max_gleanings: 1

summarize_descriptions:
  completion_model_id: default_completion_model
  prompt: "prompts/summarize_descriptions.txt"
  max_length: 500

extract_graph_nlp:
  text_analyzer:
    extractor_type: regex_english # [regex_english, syntactic_parser, cfg]

cluster_graph:
  max_cluster_size: 10

extract_claims:
  enabled: false
  completion_model_id: default_completion_model
  prompt: "prompts/extract_claims.txt"
  description: "Any claims or facts that could be relevant to information discovery."
  max_gleanings: 1

community_reports:
  completion_model_id: default_completion_model
  graph_prompt: "prompts/community_report_graph.txt"
  text_prompt: "prompts/community_report_text.txt"
  max_length: 2000
  max_input_length: 8000

snapshots:
  graphml: true
  embeddings: false

### Query settings ###
## The prompt locations are required here, but each search method has a number of optional knobs that can be tuned.
## See the config docs: https://microsoft.github.io/graphrag/config/yaml/#query

local_search:
  completion_model_id: default_completion_model
  embedding_model_id: default_embedding_model
  prompt: "prompts/local_search_system_prompt.txt"

global_search:
  completion_model_id: default_completion_model
  map_prompt: "prompts/global_search_map_system_prompt.txt"
  reduce_prompt: "prompts/global_search_reduce_system_prompt.txt"
  knowledge_prompt: "prompts/global_search_knowledge_system_prompt.txt"

drift_search:
  completion_model_id: default_completion_model
  embedding_model_id: default_embedding_model
  prompt: "prompts/drift_search_system_prompt.txt"
  reduce_prompt: "prompts/drift_search_reduce_prompt.txt"

basic_search:
  completion_model_id: default_completion_model
  embedding_model_id: default_embedding_model
  prompt: "prompts/basic_search_system_prompt.txt"
